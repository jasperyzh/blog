---
// [noise_grainy_effect](https://www.youtube.com/watch?v=_ZFghigBmqo)

// NOTE: insert component under footer to avoid breaking the primary structure
---

<svg style="display: none">
  <filter id="grainy">
    <feTurbulence type="fractalNoise" baseFrequency="0.608"></feTurbulence>
    <feComposite operator="in" in2="SourceGraphic" result="monoNoise"
    ></feComposite>
  </filter>
</svg>

<style is:global>
  body {
    position: relative;

    &:after {
      position: absolute;
      left: 0;
      top: 0;
      content: "";
      width: 100%;
      height: 100%;
      z-index: 9999;
      background: #000;
      filter: url(#grainy) brightness(0.9);
      mix-blend-mode: soft-light;
      pointer-events: none;
      opacity: 0.3;
    }
  }
</style>
